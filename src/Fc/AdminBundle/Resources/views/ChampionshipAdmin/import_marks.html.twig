{# empty Twig template #}

{% extends 'FcAdminBundle::generic_subpage.html.twig' %}


{% block content %}

<h2>Seleziona un file dei voti Gazzetta da importare nel campionato "{{ champ }}"</h2>

{% if (msg) %}
<p>{{ msg }}</p>
{% endif %}

<form action="{{ admin.generateUrl('import_marks', {'id': id}) }}" method="post" {{ form_enctype(form) }}>
    {{ form_widget(form) }}

    <input type="submit" />
</form>
{% endblock %}